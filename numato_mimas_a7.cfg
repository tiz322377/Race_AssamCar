source [find interface/cmsis-dap.cfg]
source [find target/stm32f4x.cfg]

# 开始新的调试会话前，强制执行 Cortex-M 系统复位。
# 部分 CMSIS-DAP 探针不会驱动硬件 NRST 引脚。若不执行此复位，RCC/PLL
# 寄存器可能保留上一个固件的时钟配置，导致 HAL_RCC_OscConfig() 在
# SystemClock_Config() 执行期间返回 HAL_ERROR。
cortex_m reset_config sysresetreq
